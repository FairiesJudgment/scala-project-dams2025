# Application configuration (simple)
# You can override the port with an environment variable or sbt -D
# Example: sbt -Dhttp.port=9000 run

# Placeholder: zio-http reads config from Server.default; if needed adapt startup to read this file.
app {
  http {
    port = 8080
  }
}
